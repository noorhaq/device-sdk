name: Build ESP-IDF Project

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

jobs:
  discover-examples:
    runs-on: ubuntu-latest
    outputs:
      examples: ${{ steps.set-matrix.outputs.examples }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Find examples dynamically
        id: set-matrix
        run: |
          echo "🔍 Searching for example folders..."
          # Find all example folders containing a CMakeLists.txt file
          examples=$(find esp_idf/spotflow/examples -type f -name "CMakeLists.txt" \
            | sed 's|/CMakeLists.txt||' | jq -R . | jq -s .)

          echo "Examples found:"
          echo "$examples"

          echo "examples=$examples" >> "$GITHUB_OUTPUT"

  build:
    runs-on: ubuntu-latest
    needs: discover-examples
    strategy:
      matrix:
        target: [esp32s3, esp32c6]
        example: ${{ fromJson(needs.discover-examples.outputs.examples) }}
      fail-fast: false   # continue other builds even if one fails

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: 'recursive'

      - name: Verify example path
        run: |
          if [ ! -d "${{ matrix.example }}" ]; then
            echo "❌ Path does not exist: ${{ matrix.example }}"
            exit 1
          fi
          echo "✅ Found example path: ${{ matrix.example }}"

      - name: Build with ESP-IDF
        uses: espressif/esp-idf-ci-action@v1
        continue-on-error: true    # 👈 this build won’t block others if it fails
        with:
          esp_idf_version: v5.5.1
          target: ${{ matrix.target }}
          path: ${{ matrix.example }}

      - name: Upload build artifacts
        if: always()               # 👈 upload whatever succeeded, even after failure
        uses: actions/upload-artifact@v4
        with:
          name: firmware-${{ matrix.target }}-${{ matrix.example }}
          path: ${{ matrix.example }}/build/
